investierenUI <- function(id, finanzkonto) {
  ns <- NS(id)
  tagList(
  
  )
}

investierenServer <- function(id, finanzkonto) {
  moduleServer(
    id,
    function(input, output, session) {
      
    }
  )
}